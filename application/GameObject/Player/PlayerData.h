#pragma once

enum class Direction {
    Forward,
    ForwardRight,
    Right,
    BackwardRight,
    Behind,
    BackwardLeft,
    Left,
    ForwardLeft
};

enum class MoveDirection {
    Right,
    Left,
    Forward,
    Behind
};

