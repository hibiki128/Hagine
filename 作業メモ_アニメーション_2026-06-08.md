# プレイヤー アニメーション 作業メモ (2026-06-08)

プレイヤー周りのアニメーション不具合修正と、飛行移動時の体の傾き（リーン）演出を実装した記録。
すべて Debug/x64 でビルド確認済み（エラー・警告なし。`dinput.h` の既存警告は無害）。

---

## ✅ やったこと（完了）

### 1. アニメーション基盤の不具合修正
- **コンボ攻撃の速度がImGuiで反映されない問題**
  - `Engine/3d/Animation/AnimationController.cpp` `PlayFile()`：再生ファイルパスが登録済みクリップと一致したら、そのクリップの速度・ループ・補間（ImGui/JSONで調整した値）を使うよう変更。`currentClipName_` をクリップ名にするのでクリップ一覧のハイライト・即時編集も効く。`FindClipIndexByFilePath()` を追加。
- **全アニメがループしてしまう問題**
  - `Engine/3d/Object/Object3d.cpp` `AnimationUpdate()`：補間完了後に `modelFilePath_` を更新する条件 `currentFile != nextFile` が常に偽になり、`modelFilePath_` が初期値（Idle_Ground, loop=true）に固定されていた。補間完了後は必ず `modelFilePath_ = nextFile` に更新し、クリップごとのループ設定（攻撃・ジャンプ=false 等）が効くようにした。
- **空中（飛行）上昇・下降アニメ**
  - `Application/GameObject/Player/Player.cpp` `UpdateAnimation()`：FlyMove ステートで `|velocity_.y| > kFlyVerticalAnimThreshold(0.5)` のとき `Idle_Flying`、それ以外の水平移動は `Running_Fly`。Rush は従来通り Running_Fly。

### 2. ComboSystem 定数コメント
- `Application/Utility/ComboSystem/ComboSystem.cpp` の4定数にコメント追加
  （COMBO_INTERVAL / INPUT_BUFFER_DURATION / FINAL_RETURN_DELAY / COMBO_TIMEOUT_DURATION）。

### 3. 飛行リーン（体の傾き）演出 ★メイン
- **エンジン：描画専用の回転オフセット**
  - `Engine/3d/Object/Base/BaseObject.h/.cpp` に `SetRenderRotationOffset(quat, pivot)` / `ClearRenderRotationOffset()` を追加。
  - `Draw()` 内で一時的に回転を合成し描画後に元へ戻すため、**ゲームプレイ用の向き（射撃・前方ベクトル）は汚さない**。
  - モデル中心が原点にない問題に対応するため、**ピボット（回転中心）補正**付き。
- **Player：`UpdateFlyLean()`**
  - ロックオン中の FlyMove のみ、顔（向き）は敵に向けたまま体を進行方向へ傾ける。
  - 左右＝**Y軸ヨー**（真横まで）、前後＝ピッチ（前進＝前傾／後退＝仰向け）、**ロールなし**。
  - 平滑化は**クォータニオン Slerp**。
  - 傾きは「体の水平右方向（`GetRight()` を水平化）」軸まわりに構築し、**向き非依存**にした（→技術メモ参照）。
  - ImGui「飛行リーン」で調整可、Player の Save/Load で永続化。

### 4. パッド地上移動アニメの左右反転
- `Player.cpp` `DirectionUpdate()` ゲームパッド分岐：方向分類用の X を `-GetLeftStickX()` → `+GetLeftStickX()` に変更し、キーボード（D=Right）と左右を一致させた（移動量・移動方向には影響なし）。

---

## ⏳ 残タスク / 確認してほしいこと

1. **【要・実機確認】飛行リーン 後退の符号**
   - 後退時に「仰向け」ではなく「前のめり」に反転して見える可能性あり（その場合は全方向で一貫して反転するはず）。
   - 反転していたら `UpdateFlyLean()` の `pitch` 符号を1か所反転で直る。必要ならImGuiに反転トグルを追加する。
2. **飛行リーン 全体の見た目調整**
   - ImGui「飛行リーン」の各角度／基準速度／追従速度、特に **「回転中心(ピボット)」** をモデル中心に合わせて調整
     （モデルが Blender Z 方向に +1 ほど浮いているため、原点回転だと位置がずれる）。
3. **【任意・"尚更いい"】上半身だけ起き上がる表現**
   - 未実装。背骨（mixamorig:Spine 系）ボーンを手続き的に曲げる必要があり、
     `Bone` / `ModelAnimation` / `Object3d` / `BaseObject` に「ボーン回転オフセット」の仕組みを追加する想定。
   - ボーン名・軸・角度はリグ依存なので、ImGuiで調整可能にして実装するのが安全。

---

## 🔧 技術メモ（重要・今後のため）

**このエンジンのクォータニオン→行列規約は「共役」**。
- `v·M(q) = Rotate_{q⁻¹}(v)`（行ベクトル `v·M`、`QuaternionToMatrix4x4` 使用）。
- 検証：Z軸 +θ で `(1,0,0) → (cosθ, −sinθ, 0)`（標準は +sinθ）。
- **影響1**：単軸回転は符号が反転して見える（`FromAxisAngle` の角度を反転して合わせる必要がある）。
- **影響2**：合成は `M(a*b) = M(a)·M(b)`。`aim * lean` は「敵を向いた後に**ワールド固定軸**で lean」になり、
  ローカル傾けにならない。体のローカル軸で傾けたいときは、傾きを**体の実ワールド軸（GetRight 等）で構築**するか、
  合成順を入れ替える必要がある。←今回の後退ブレ不具合の原因はこれ。

---

## 📍 主な調整箇所

- **ImGui**：プレイヤー Debug →「飛行リーン」
  （有効 / 前傾の最大角 / 仰け反りの最大角 / 横移動のヨー最大角 / 基準速度 / 追従速度 / 回転中心(ピボット)）
- **JSON**：`EntityData/Player` に `flyLean*` キー保存
  （flyLeanEnabled, flyLeanMaxFwdPitchDeg, flyLeanMaxBackPitchDeg, flyLeanMaxSideDeg, flyLeanRefSpeed, flyLeanResponse, flyLeanPivot）
- **定数**：`Player.h` `kFlyVerticalAnimThreshold`（空中の上昇/下降アニメ切替閾値, 0.5）

---

## 📄 変更ファイル一覧

- `Engine/3d/Animation/AnimationController.h` / `.cpp`
- `Engine/3d/Object/Object3d.cpp`
- `Engine/3d/Object/Base/BaseObject.h` / `.cpp`
- `Application/GameObject/Player/Player.h` / `.cpp`
- `Application/Utility/ComboSystem/ComboSystem.cpp`
